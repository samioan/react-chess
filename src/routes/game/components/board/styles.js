import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
  board: {
    width: "100%",
    height: "100%",
  },
  rowContainer: {
    height: "12.5%",
  },
}));

export { styles };
export default styles;
